# 🍎 水果價格預測系統 - 全棧部屬實作案

本專案為因應企業技術挑戰，於 **12 天內從零自學 PHP** 並完成開發之實作案。專案整合了 MySQL 資料庫、RESTful API 開發、以及前端數據監控儀表板，並部屬至 GCP (Google Cloud Platform) 雲端環境。

---

## 📊 視覺化監控儀表板 (Live Demo)
為了直觀呈現數據分析與預測結果，本系統提供兩套動態儀表板：

### 1. 商業智慧分析 (Tableau)
提供互動式數據探索，適合深入分析歷史趨勢與跨維度比較。
- **🔗 展示連結：** [Tableau Public Dashboard](https://public.tableau.com/shared/XYKXQMWNJ?:display_count=n&:origin=viz_share_link)

### 2. 即時價格預測系統 (PHP)
本專案開發之全端系統，整合後台管理與即時預測數據。
- **🔗 展示連結：** [PHP 預測系統儀表板](http://35.189.163.87:8081/)
- **登入帳號：** `tjr103-team02`
- **登入密碼：** `password`
- **核心功能：** 支援多種作物趨勢切換、平均價格監控、全台產量分佈地圖。

---

## 🛠️ 技術規格 (Backend API)
系統提供一套完整的 RESTful API 接口，支援以 **作物名稱 (crop_name)** 直接進行數據管理，無需處理繁瑣的 ID 轉換。

- **Base URL:**
  ```
  http://35.189.163.87:8081/api/v1/maintenance
  ```
- **回應格式:** `application/json` (UTF-8)

---

## 📡 API 接口總覽 (CRUD)

| 功能 | Method | Endpoint | 關鍵參數 | 說明 |
| :--- | :--- | :--- | :--- | :--- |
| **查詢 (Read)** | `GET` | `/prices` | `crop_names[]`, `start_date`, `end_date` | 取得指定範圍內的價格數據 (建議測試 2025 年資料) |
| **新增 (Create)** | `POST` | `/prices` | `date`, `crop_name`, `price`, `mode` | 插入新紀錄 (測試請用 2099 年避免污染數據) |
| **更新 (Update)** | `PUT` | `/prices` | `date`, `crop_name`, `mode`, `price` | 基於複合鍵更新指定紀錄的價格 |
| **刪除 (Delete)** | `DELETE` | `/prices` | `date`, `crop_name`, `mode` | 基於複合鍵移除特定數據 |

---

## 📝 呼叫範例

### 1. 數據查詢 (使用 2025 現有資料範例)
為了確保能獲取真實資料，查詢建議以 2025 年為範圍。
您可以直接將下方完整網址複製到 Chrome 瀏覽器，或直接點擊連結查看結果：

- **完整測試連結 ：** [點我直接執行 GET 查詢](http://35.189.163.87:8081/api/v1/maintenance/prices?crop_names[]=柳橙&start_date=2025-01-01&end_date=2025-12-31)
- **複製連結：**
  ```
  http://35.189.163.87:8081/api/v1/maintenance/prices?crop_names[]=柳橙&start_date=2025-01-01&end_date=2025-12-31
  ```
- **請求格式：** `Base URL` + `/prices` + `參數`

### 2. 數據維護 (使用 2099 測試日期範例)
由於瀏覽器網址列僅支援 GET 請求，**新增 (POST)、更新 (PUT)、刪除 (DELETE)** 操作請使用 **Postman** 或直接複製以下 **curl 指令** 於終端機執行：

> **💡 Windows 使用者提示：** 由於終端機編碼差異，若執行後回傳「找不到作物」或亂碼，建議直接使用 **Postman** 進行測試，以確保中文參數能正確傳遞。

- **新增數據 (POST):**
```bash
curl -X POST "http://35.189.163.87:8081/api/v1/maintenance/prices" \
     -H "Content-Type: application/json" \
     -d '{
           "date": "2099-01-03",
           "crop_name": "柳橙",
           "price": 45.5,
           "mode": "prediction"
         }'
```

- **更新數據 (PUT):**
```bash
curl -X PUT "http://35.189.163.87:8081/api/v1/maintenance/prices" \
     -H "Content-Type: application/json" \
     -d '{
           "date": "2099-01-03",
           "crop_name": "柳橙",
           "mode": "prediction",
           "price": 42.8
         }'
```

- **刪除數據 (DELETE):**
```bash
curl -X DELETE "http://35.189.163.87:8081/api/v1/maintenance/prices" \
     -H "Content-Type: application/json" \
     -d '{
           "date": "2099-01-03",
           "crop_name": "柳橙",
           "mode": "prediction"
         }'
```